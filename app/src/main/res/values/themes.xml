<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kindergartenworkshop" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="android:textColor">@color/purple_text_main</item>
         <item name="android:fontFamily">@font/inika</item>
         <item name="android:textSize">@dimen/text_size_small</item>
    </style>

    <style name="Theme.Kindergartenworkshop" parent="Base.Theme.Kindergartenworkshop" />
</resources>